/**
 * OpenTelemetry initialization helpers (shared across Node/browser runtimes).
 *
 * Currently exports a version placeholder; future expansions include
 * standardized tracer/provider setup and common resource attributes.
 *
 * @internal
 */
export const version = '0.1.0'
